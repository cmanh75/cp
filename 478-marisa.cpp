//problem "478-marisa"
//created in 21:42:11 - Wed 06/11/2024
#include<bits/stdc++.h>
using namespace std;

const int N = 1e5 + 5;
int q, n, a[N];
stack < int > st;

void solve() {
    st.pop();
    // cout << st.top();
}

int main() {
    ios_base::sync_with_stdio(false);
    cin.tie(NULL); cout.tie(NULL);
    #define task "test"
    if(fopen(task ".inp", "r")) {
        freopen(task ".inp", "r", stdin);
        freopen(task ".out", "w", stdout);
    }
    int test = 1;
    while (test--) {
        solve();
    }
}